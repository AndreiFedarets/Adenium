<v:View x:Class="Adenium.Sample.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:v="clr-namespace:Adenium.Views;assembly=Adenium"
        Height="500" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="Open Grid" Command="{Binding OpenGridViewCommand}" />
        <Button Grid.Row="1" Content="Open Tab" Command="{Binding OpenTabViewCommand}" />
    </Grid>
</v:View>
