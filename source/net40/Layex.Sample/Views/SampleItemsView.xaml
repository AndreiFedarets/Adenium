<v:View x:Class="Layex.Sample.Views.SampleItemsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:v="clr-namespace:Layex.Views;assembly=Layex"
        x:Name="View" Height="600" Width="600" DisplayMode="Grid">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="Reset" x:Name="ResetItems" />
            <ComboBox Grid.Column="2" ItemsSource="{Binding DisplayModes}" SelectedItem="{Binding DisplayMode, ElementName=View}" />
        </Grid>
        <ContentControl x:Name="ViewContent" Grid.Row="1" />
    </Grid>
</v:View>
